/**
 * @deprecated HTTP handlers live in `src/modules/branch/controller/staff-assignment.controller.js`.
 * Re-export shim so existing routes remain unchanged.
 */
module.exports = require('../src/modules/branch/controller/staff-assignment.controller');
