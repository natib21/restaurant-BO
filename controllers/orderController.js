/**
 * @deprecated HTTP handlers live in `src/modules/orders/controller/order.controller.js`.
 * Re-export shim so existing routes remain unchanged.
 */
module.exports = require('../src/modules/orders/controller/order.controller');
